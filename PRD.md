# 产品需求文档（PRD）— DocGraph：本地文档知识图谱生成与可视化工具

> 一句话：把用户拖入的本地文档（论文 / 报告 / 笔记）自动解析为**实体与关系**，一键生成可交互的**知识图谱**，以 **Windows 单文件 exe** 提供「打开即用」的桌面体验。
>
> 定位：GitHub 开源 · 本地优先 · LLM 驱动 · 面向学术/研究场景

---

## 0. 文档信息

| 项目 | 内容 |
|---|---|
| 产品名称 | **DocGraph**（已定） |
| 文档版本 | v0.1（草案，待评审） |
| 状态 | Draft |
| 目标平台 | Windows 10/11 x64（后续 macOS / Linux） |
| 分发方式 | GitHub Releases 发布单文件 exe（PyInstaller 打包） |
| 技术栈方向 | Python 后端 + 内嵌 Web 前端 + LLM API 抽取 |

**评审要点（本次 PRD 已确认的关键决策）**

1. 图谱核心对象：**实体级知识图谱**（抽取概念/方法/人物/文献等实体及其关系）
2. 抽取方式：**必须 LLM API**（OpenAI 兼容协议，效果优先，需联网 + 用户自配 API Key）
3. 技术栈：Python + Web 前端（pywebview）+ PyInstaller 打包单 exe
4. 目标用户：**学术 / 研究场景**（论文、文献综述、笔记）
5. 范围优先级：**先做单文件（Word / PDF）的图构建**，批量导入与更多格式后置（V1.1）
6. 增量适应与分组：用户可将文件**分组**，同一组采用同一套实体/关系抽取方式（类型表、参数）；新文件加入时在**组内**完成实体对齐并融入该组图谱，不破坏原图（见 FR-310 / FR-311）
7. 文档身份：**不允许重名文档**，所有文档视为独立文档（内容相同也互不合并）；跨分组复用同一源文档时**自动复制并重命名为不重复文件名**（见 FR-310）

---

## 1. 产品概述

### 1.1 背景与问题

研究场景中，知识以大量异构文档（PDF 论文、报告、Markdown 笔记、Word 草稿）形态散落：

- 文献量大，信息割裂，难以建立**跨文档的全局认知**；
- 手动做笔记、用文件夹管理，**实体与实体之间的关系隐性丢失**；
- 已有工具各有短板：
  - **Obsidian / Logseq**：依赖用户手动建链，不自动抽取；
  - **NotebookLM**：需上传云端，隐私与合规受限，且非图谱形态；
  - **Connected Papers / ResearchRabbit**：只覆盖公开论文库，不支持任意本地文档；
  - **Zotero**：文献管理强，但无知识图谱能力。

**机会点**：一个本地、隐私、LLM 驱动的「文档 -> 实体级知识图谱」工具，以 exe 形式零门槛交付。

### 1.2 产品价值主张

| 价值 | 说明 |
|---|---|
| 自动 | 拖入文档即抽取，无需手工建链 |
| 可校验 | 每个实体/关系附**原文证据**与置信度，支持人工修正 |
| 隐私 | 数据全部本地，仅抽取时调用用户自配的 LLM API |
| 零门槛 | 单文件 exe，双击即用，无环境依赖 |
| 可导出 | 图谱/数据可导出为图片、JSON、CSV，衔接 Gephi 等工具 |

### 1.3 产品愿景

从「文档管理工具」进化为「研究者的领域知识地图」：输入一批文档，输出一张可浏览、可追溯、可持续维护的知识图谱，让研究者把时间花在读图和思考上，而不是翻文件。

---

## 2. 目标用户与使用场景

### 2.1 用户画像

| 画像 | 特征 | 核心诉求 |
|---|---|---|
| **P1 研究生 / 科研人员（核心）** | 阅读大量论文与文献，写综述 | 快速梳理领域脉络：谁提出了什么、谁基于谁、谁比较了谁 |
| **P2 技术写作者 / 产品经理（次级）** | 文档多而杂，需要结构化 | 把散乱资料变成一张图，辅助汇报与写作 |
| **P3 知识管理爱好者（次级）** | 长期维护个人笔记库 | 笔记自动沉淀为知识网络 |

### 2.2 核心使用场景

**场景 A：文献调研（首要）**
> 王同学要写「图神经网络」综述。他把 50 篇 PDF 拖进 DocGraph，配置好 DeepSeek API Key，点击「开始抽取」。30 分钟后得到一张领域图谱：方法（GCN/GAT/GraphSAGE）为节点，带「提出/改进/基于」关系，点开 GCN 看到来源论文与原文摘录。他按「方法」类型筛选，快速定位到演化主线。

**场景 B：论文写作查证**
> 写引言时想确认「谁最早提出注意力机制」，在搜索框输入关键词，图谱高亮相关实体与其关系，详情面板给出带页码的证据原文，直接引用。

**场景 C：笔记沉淀**
> 把多年 Markdown 笔记批量导入，生成个人知识图谱；定期重新抽取增量文档，图谱持续生长。

### 2.3 用户故事（User Stories）

- 作为研究者，我希望拖拽多个 PDF 就能批量导入，以便快速开始。
- 作为研究者，我希望点击图中的实体能看到它的描述、别名和原文证据，以便追溯结论来源。
- 作为研究者，我希望按实体类型/关系类型/来源文档筛选图谱，以便聚焦某条研究脉络。
- 作为研究者，我希望导出高清图片和 CSV 节点/边表，以便放进论文和用 Gephi 深入分析。
- 作为用户，我希望 API 配置一次后长期可用，且 Key 安全存储，以便减少重复配置。

---

## 3. 产品范围

### 3.1 MVP（V1.0）范围

| 能力域 | 内容 |
|---|---|
| 分发 | Windows 单文件 exe，双击运行，无需安装 Python |
| 文档导入 | **单文件优先**：拖拽 / 文件选择导入单个文档；MVP 支持 **PDF（文本型）/ DOCX**，先打通单文件图构建全链路 |
| 解析 | 文本提取、按章节分块、保留来源定位（文档 + 页码 + 段落） |
| 抽取 | LLM 抽取**实体与关系**，OpenAI 兼容 API（可配 base_url / key / model），JSON Schema 结构化输出 |
| 文件分组 | 用户可对文档**分组**，每组独立配置实体/关系类型表与抽取参数，图谱按组展示；文档名唯一，跨组复用自动复制重命名 |
| 增量适应 | 新文件加入**某分组**时，抽取结果与该组既有实体/关系**自动对齐合并**，新类型提示确认后纳入该组类型表 |
| 图谱 | 实体=节点、关系=有向边，去重合并，置信度 + 证据 |
| 可视化 | 力导向布局、缩放平移、类型着色、邻居高亮、详情面板、筛选、搜索 |
| 导出 | PNG/SVG、Graph JSON、CSV（节点/边表）、Markdown 摘要报告 |
| 持久化 | 项目保存/打开、抽取断点续跑、人工校验记录留存 |
| 设置 | API 配置（含 Key 安全存储）、抽取参数、语言（中/英） |

**MVP 里程碑拆解（按开发顺序）**

| 里程碑 | 内容 | 完成标志 |
|---|---|---|
| M1 单文件图构建 | 单文件 PDF/DOCX 导入 -> 解析分块 -> LLM 抽取 -> 实体/关系图谱渲染（默认组） | 单文件全链路可用，3 步出图 |
| M2 文件分组与组级配置 | 分组管理 + 每组独立实体/关系类型表与抽取参数 | FR-310 验收通过 |
| M3 图谱交互 | 筛选 / 搜索 / 下钻 / 详情 / 导出（按组切换视图） | 交互功能按验收标准通过 |
| M4 新文件增量适应 | 新文件加入分组，组内实体对齐合并不破坏原图 | FR-311 验收通过 |
| M5 交付打磨 | 单 exe 打包、断点续跑、设置、中英界面 | 全新 Windows 机器双击 exe 全流程可用 |

### 3.2 非目标（MVP 明确不做）

- 批量导入与多格式支持（MD/TXT/HTML，V1.1）
- 扫描版 PDF 的 OCR（V1.1 再评估，MVP 仅检测并提示）
- 团队协作、多人共享、Web 版
- 本地模型（Ollama 等）接入（V2.0）
- 自动更新、代码签名（V1.1+，避免影响 MVP 发布节奏）
- 图谱的在线发布/分享链接

---

## 4. 功能需求

> 优先级：**P0** = MVP 必须，**P1** = V1.1，**P2** = V2.0。
> 验收标准均为可测试的客观行为。

### 4.1 文档导入与管理

**FR-101 单文件导入（P0，MVP）**
- 描述：支持拖拽或文件选择对话框导入**单个**文档（PDF / DOCX）；MVP 聚焦单文件场景：导入 -> 解析 -> 抽取 -> 出图全链路可用。
- 验收：拖入单个 PDF 或 DOCX 可完成全流程；**与项目内已有文档同名的文件被拒绝并提示改名**；内容相同但文件名不同的文件作为独立文档正常导入（视为不同文档）；不支持的格式给出明确提示而非静默失败。

**FR-102 支持格式（P0，MVP）**
- 描述：MVP 支持 **PDF（文本层）** 与 **DOCX** 两种格式；单文件建议 ≤ 50 MB。
- 验收：PDF 与 DOCX 样例均可正确提取文本并进入抽取流程；其余格式提示「暂不支持，将在后续版本支持」。

**FR-103 文档状态机（P0）**
- 描述：每个文档有状态：`待处理 -> 解析中 -> 已解析 -> 抽取中 -> 已抽取 -> 失败`，失败附原因（解析失败/API 错误/超时/用户取消）。
- 验收：界面实时展示各文档状态；失败文档可单独重试。

**FR-104 文档元数据（P1）**
- 描述：由 LLM 从文档首部自动识别标题/作者/年份/摘要，或从文件名推断；元数据可手动编辑。
- 验收：PDF 首屏信息被正确识别且可编辑；编辑后持久化。

**FR-105 文档操作（P0）**
- 描述：文档列表支持搜索、按状态筛选、删除（级联删除其抽取结果）、重新抽取。
- 验收：删除文档后图中对应实体/关系消失；重新抽取只重建该文档相关子图。

**FR-106 批量导入与多格式（P1，V1.1）**
- 描述：支持拖拽文件夹、批量文件选择，一次导入 ≥ 50 个文档；扩展 MD / TXT / HTML 格式；与单文件流程复用同一解析与抽取管线。
- 验收：一次拖入 ≥ 50 个文件全部进入文档列表并完成抽取；同名冲突的文件自动重命名（如「报告 (2).pdf」）并汇总提示；各新增格式样例可正常出图。

### 4.2 文档解析与预处理

**FR-201 文本提取（P0）**
- 描述：PDF 用 pdfplumber 提取文本与页码；DOCX/MD/TXT/HTML 分别用对应解析器；编码自动检测。
- 验收：标准样例（含中文）文本提取无乱码；页码信息保留到段落级别。

**FR-202 章节分块（P0）**
- 描述：按标题层级/段落切分；每块 ≤ 3000 tokens，相邻块重叠 200 tokens；每块携带来源定位（文档 ID + 页码 + 块序号）。
- 验收：任一文本块在详情中可一键定位回原文位置。

**FR-203 扫描 PDF 检测（P0）**
- 描述：检测到文本量极低的 PDF（疑似扫描件）时，标记为「疑似扫描件，需 OCR（暂不支持）」，不进入抽取。
- 验收：扫描版样例被正确标记并给出可理解的提示。

### 4.3 LLM 实体与关系抽取【核心】

**FR-301 Provider 抽象（P0）**
- 描述：支持任意 OpenAI 兼容协议的 LLM（OpenAI / DeepSeek / 通义 / Kimi / 自建网关等），通过 `base_url + api_key + model` 配置；预留非兼容 provider 扩展点。
- 验收：切换 DeepSeek 与 OpenAI 配置均能完成一次完整抽取；配置缺失时引导到设置页。

**FR-302 实体类型定义（P0，学术场景）**
- 描述：默认实体类型：`概念/方法/理论`、`人物`、`组织/机构`、`论文/文献`、`数据集/工具`、`事件`、`指标`；类型表**按分组独立配置**（见 FR-310）；新文件发现的新类型可动态扩展（经用户确认，见 FR-311）。
- 验收：抽取结果中实体必须带有类型字段且取值在类型表内。

**FR-303 关系类型定义（P0）**
- 描述：默认关系类型：`提出`、`基于`、`改进`、`比较`、`引用`、`验证`、`评价`、`应用于`、`属于`；关系为有向；关系类型表**按分组独立配置**（见 FR-310），并同样支持随新文件动态扩展。
- 验收：关系必须带 source、target、type、evidence、confidence 五个字段。

**FR-304 结构化输出约束（P0）**
- 描述：使用 JSON Schema / function calling 约束 LLM 输出；temperature 固定 0.1~0.3；输出校验失败自动重试（最多 2 次），仍失败则记录该块失败并继续。
- 验收：10 个随机文本块的抽取结果 100% 可解析为合法 JSON；非法输出被捕获并重试，不中断整体流程。

**FR-305 抽取流水线（P0）**
- 描述：分块 -> 逐块调用 LLM -> 解析校验 -> **块内/块间合并去重** -> **与所在分组既有图谱实体对齐**（新文件场景，见 FR-311）-> 回填文档关联 -> 写入图谱。
- 验收：同一实体出现在多个文档/分块中，最终图中只保留一个节点并聚合其全部证据。

**FR-306 去重与合并（P0）**
- 描述：同名合并；别名归一（LLM 抽取时输出 canonical name + aliases）；相似名称（编辑距离/向量相似度超阈值）自动合并，低置信度冲突进入「待确认」列表；该机制同时作用于块间合并与「新文件 vs 同组既有图谱」对齐两个层面。
- 验收：「Graph Neural Network」与「GNN」被识别为同一实体；「attention mechanism」与「Attention Mechanism」合并。

**FR-307 证据与置信度（P0）**
- 描述：每个实体/关系携带：`confidence（0-1）`、`evidence[]`（原文摘录 + 来源文档 + 页码）、`aliases[]`、`description`。
- 验收：图谱详情面板能展示上述全部字段；证据摘录与原文一致（抽样比对 ≥ 95%）。

**FR-308 成本与限流控制（P0）**
- 描述：分块级结果缓存（相同文本不重复调用）；并发数可配（默认 3）；自动重试 + 指数退避应对限流；实时展示预计消耗与进度；支持暂停/继续/取消。
- 验收：中断后重新打开项目，已完成的块不再重复计费调用；取消后已抽取结果保留。

**FR-309 人工校验（P1）**
- 描述：实体/关系列表支持增、删、改（改名/改类型/改关系）、手动合并、标记「忽略」；校验结果持久化，重新抽取时保留人工修改、不覆盖。
- 验收：对实体改名后重新抽取，名称保持人工设定；标记忽略的实体不再出现在新图谱中。

**FR-310 文件分组与组级抽取配置（P0）**
- 描述：用户可创建 / 重命名 / 删除分组，并将文档归入分组（拖拽或批量设置）；未分组文档归入「默认组」；**一个文档属于一个分组**；删除分组时文档移回默认组。**文档身份规则**：① 项目内文档名（文件名）**唯一，不允许重名**；② 所有文档一律视为独立文档（即使内容相同也是不同文档，抽取结果互不影响）；③ 若用户需在多个分组复用同一源文档，应用**自动复制一份并重命名为不重复的文件名**（如「报告.pdf」->「报告 (2).pdf」），副本作为独立文档归入目标分组，与原件分别抽取、分别管理。每组独立维护：**实体类型表、关系类型表、抽取参数**（默认继承全局配置，可覆盖）。图谱画布按当前分组展示（组内图谱只含该组文档的实体与关系）；提供「全部」跨组聚合视图。
- 验收：① 分组增删改即时生效并持久化；② 同一文档可在分组间移动，移动后抽取配置随组切换；③ 切换到某分组后画布与统计只含该组数据；④ 「全部」视图正确聚合所有分组；⑤ 导入与项目内已有文档同名的文件被拒绝并提示改名；⑥ 将同一源文档归入第二个分组时，自动生成不重名副本（如「报告 (2).pdf」）并归入该组，原件与副本各自独立抽取、互不影响；⑦ 副本命名始终避开项目内已有文档名。

**FR-311 新文件增量适应（组内实体对齐）（P0）**
- 描述：向既有项目的**某个分组**追加新文件时：① 仅对新文件执行解析与抽取；② 抽取前向模型注入「已知实体上下文」（**同组**既有图谱高频实体与相似候选列表），引导模型复用既有 canonical name；③ 抽取结果经名称归一、别名命中、相似度匹配与**同组**图谱对齐合并；④ 新文件发现的新实体类型/关系类型提示用户确认后纳入**该组**类型表，供该组后续文件使用。
- 验收：① 向含 100 实体的分组加入新文件，指代该组既有实体的名称 ≥ 90% 对齐到既有节点而非新建重复节点；② 新文件中的全新概念作为新节点加入且不与既有节点误合并；③ 新关系正确连接同组既有实体与新实体；④ 新类型出现时被识别并提示确认，确认后仅该组后续抽取可用该类型；⑤ 跨组不产生对齐（不同分组的同名实体保持独立，互不合并）。

### 4.4 图谱构建

**FR-401 图模型（P0）**
- 描述：节点 = Entity，有向边 = Relation；每个实体记录来源文档集合；图数据用 networkx 构建后序列化。
- 验收：图谱 JSON 结构稳定、可版本化，能完整反序列化回图。

**FR-402 统计信息（P0）**
- 描述：展示节点数、边数、连通分量数、Top 10 高连接度实体。
- 验收：数字与图实际渲染一致。

### 4.5 图谱可视化与交互【核心】

**FR-501 画布基础（P0）**
- 描述：缩放、平移、自适应视图、双击节点居中。
- 验收：5 万节点/20 万边规模下拖拽帧率 ≥ 30fps（按需渲染/LOD）。

**FR-502 布局（P0）**
- 描述：默认力导向布局；提供环形、层级布局切换。
- 验收：三种布局可切换，布局切换后节点/边不丢失。

**FR-503 节点与边样式（P0）**
- 描述：节点按实体类型着色 + 图标，尺寸随度数增大；边按关系类型着色，方向用箭头表示。
- 验收：图例与样式一一对应；类型筛选后图例同步更新。

**FR-504 交互（P0）**
- 描述：悬停显示摘要；点击节点高亮其邻居并淡化其余；拖拽节点临时固定；节点/边可框选。
- 验收：高亮/淡化逻辑正确，交互响应 < 100ms。

**FR-505 详情面板（P0）**
- 描述：选中实体显示：名称、类型、别名、描述、置信度、证据列表（可点击跳转到原文文档位置）、关联实体；选中边显示关系详情。
- 验收：详情字段完整；证据点击能打开对应文档并定位到段落。

**FR-506 筛选（P0）**
- 描述：按实体类型、关系类型、来源文档、最小度数筛选；筛选结果实时更新。
- 验收：组合筛选结果与图一致。

**FR-507 搜索（P0）**
- 描述：实体名称模糊搜索，结果高亮定位。
- 验收：输入子串可命中别名；无结果时有空态提示。

**FR-508 文档下钻（P0）**
- 描述：文档列表点击某文档，画布切换为该文档内实体子图，并标注文档名。
- 验收：子图只含该文档涉及的实体与关系；返回全局图无状态残留。

**FR-509 路径分析（P1）**
- 描述：任意两实体间最短路径高亮。
- 验收：选择两个实体后，图上高亮一条（或多条并列）路径，附路径说明。

### 4.6 导出

**FR-601 图片导出（P0）**
- 描述：导出当前视图为 PNG（≥ 2x 分辨率）、SVG。
- 验收：导出图片清晰，中文标签正常。

**FR-602 数据导出（P0）**
- 描述：导出 Graph JSON（自描述 schema）、CSV 节点表 + 边表（含证据列，可导入 Gephi）。
- 验收：CSV 用 Gephi 可成功导入并还原节点与边。

**FR-603 摘要报告（P1）**
- 描述：导出 Markdown 报告：项目概览、Top 实体、按类型分组的实体清单、带证据的关键关系列表。
- 验收：报告包含 FR-402 统计数据与 FR-307 关键字段。

### 4.7 项目持久化

**FR-701 项目结构（P0）**
- 描述：项目 = 配置文件 + 文档副本/引用 + 抽取结果（SQLite）+ 图谱快照（JSON）；保存到用户指定目录。
- 验收：项目目录可整体拷贝到另一台机器打开，内容不丢失。

**FR-702 保存与恢复（P0）**
- 描述：手动保存、关闭前自动保存、最近项目列表；抽取中断后重开可断点续跑。
- 验收：模拟崩溃后重开，已抽取文档不重复计费、人工校验记录完整。

### 4.8 设置

**FR-801 API 配置（P0）**
- 描述：base_url、api_key、model、temperature、并发数、预算上限（单次抽取费用估算与硬性停止阈值）。
- 验收：配置后可在「连接测试」中一键验证；预算超限自动停止并提示。

**FR-802 API Key 安全存储（P0）**
- 描述：Key 经 Windows Credential Manager（keyring 库）存储，日志与配置文件中不出现明文。
- 验收：抓取日志/配置文件无 Key 明文；换用户登录后 Key 不可见。

**FR-803 语言与主题（P1）**
- 描述：界面中/英切换；明/暗主题。
- 验收：切换即时生效并持久化。

### 4.9 分发与更新（P1+）

**FR-901 单 exe 分发（P0）**
- 描述：PyInstaller 打包单文件 exe，双击即用；不要求用户预装 Python 或 WebView2（随包处理运行时）。
- 验收：全新 Windows 10 虚拟机双击 exe 可完成「导入 -> 抽取 -> 出图」全流程。

**FR-902 检查更新（P1）**
- 描述：启动时静默检查 GitHub Releases，有新版本提示下载。
- 验收：发布新版本后旧版本能检测到并给出下载链接。

---

## 5. 非功能需求

| 类别 | 需求 | 指标 |
|---|---|---|
| 性能-解析 | 文本型 PDF（≤ 50 页）解析 | ≤ 5s（本地 CPU） |
| 性能-抽取 | 并发 3 时单文档抽取 | ≈ LLM 单块耗时 × 块数 / 3 |
| 性能-渲染 | 图谱交互 | 5 万节点/20 万边，拖拽 ≥ 30fps（LOD/按需渲染） |
| 性能-搜索 | 实体搜索 | ≤ 500ms |
| 兼容性 | 平台 | Windows 10/11 x64 优先；架构上预留 macOS/Linux |
| 隐私 | 联网范围 | 除用户配置的 LLM API 外**零联网**；设置页提供「将发送的数据」预览 |
| 可靠性 | 崩溃恢复 | 任意阶段中断不损坏项目；重启可续 |
| 可用性 | 新手路径 | 3 步出图：拖入文档 -> 开始抽取 -> 查看图谱 |
| 可维护性 | 扩展性 | 解析器、抽取 provider、实体/关系类型均可插件化扩展；核心逻辑单测覆盖率 ≥ 70% |
| 安全 | 供应链 | 依赖锁定 + 构建产物哈希；后续代码签名减少 SmartScreen 拦截 |

---

## 6. 信息架构与交互流程

### 6.1 主界面布局（线框描述）

```
+---------------------------------------------------------------+
| 工具栏: [导入文档] [开始抽取] [项目▼ 保存/打开] [导出▼] [设置] [语言]  |
+---------------+-------------------------------+-----------------+
| 左侧: 分组+文档树 |       中部: 图谱画布             | 右侧: 详情面板   |
|  +新建分组/搜索   |   缩放/平移/布局切换/筛选栏       |  实体信息       |
|  机器学习论文     |   （力导向图, 节点/边着色,         |  证据列表       |
|    文档A ✓已抽取 |    按当前分组展示）              |  关联实体       |
|  医疗文献         |                                |  操作: 修正     |
|    文档B ⏳抽取中 |                                |                |
|  文档C ✗失败(默认组)|                              |                |
+---------------+--------------------------------+-----------------+
| 状态栏: 抽取进度 x/y · 节点 N · 边 M · 预估成本 ¥ · [暂停/继续]      |
+---------------------------------------------------------------+
```

### 6.2 核心用户流程

**首次使用**
> 双击 exe -> 欢迎页 -> 设置 API（base_url/key/model，一键测试）-> 新建项目 -> 进入主界面。

**日常主流程（3 步出图）**
> 拖入文档（或选择文件夹）-> 点「开始抽取」（实时进度、可暂停/取消）-> 图谱自动渲染 -> 筛选/搜索/下钻 -> 导出图片或数据。

**人工校验流程**
> 选中实体/关系 -> 详情面板查看证据 -> 改名/合并/忽略 -> 重新抽取（人工修改保留）。

**异常流程**
> API 限流/失败 -> 自动重试与退避 -> 仍失败标记「失败」可重试 -> 预算超限自动停止并提示。

### 6.3 状态与错误提示规范

- 所有耗时操作必须显示进度并可取消；错误分级：`提示（可忽略）/警告（需确认）/错误（阻塞，附原因与建议操作）`。
- 提示文案使用「发生了什么 + 如何解决」句式，不出现堆栈。

---

## 7. 技术方案建议

### 7.1 总体架构

```
+-------------------------- 用户侧 Windows --------------------------+
|  DocGraph.exe（PyInstaller 单文件）                                  |
|  +----------------------------------------------------------------+  |
|  |  pywebview（桌面壳，内嵌 WebView2）                               |  |
|  |  +---------------+         +-------------------------------+    |  |
|  |  | Web 前端 (Vue3)|<-JSBridge->| Python 后端 (内置 HTTP 服务)  |    |  |
|  |  | 图谱渲染/交互   |         | 路由: import/parse/extract/... |    |  |
|  |  +---------------+         +---------------+----------------+    |  |
|  |       模块: 解析器/抽取器/图构建/存储      |                      |  |
|  +------------------------------------------+----------------------+  |
|      项目目录(SQLite+JSON) · keyring(Credential Manager)            |
+---------------------------------------------+------------------------+
                                              | HTTPS（仅抽取调用，用户配置）
                                       +------v-------+
                                       | LLM API      |
                                       | (OpenAI 兼容) |
                                       +--------------+
```

### 7.2 技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| 语言/运行时 | Python 3.10+ | 文档解析生态最全 |
| 桌面壳 | pywebview（WebView2） | 体积小、无需随包浏览器 |
| 文档解析 | pdfplumber、python-docx、markdown、beautifulsoup4、charset-normalizer | 成熟稳定，保留页码/结构 |
| LLM 调用 | openai SDK（OpenAI 兼容）+ 自研 provider 适配层；JSON Schema / function calling | 一套代码兼容 DeepSeek/OpenAI/通义等 |
| 图构建/分析 | networkx | 构建、合并、路径/中心度分析 |
| 图可视化 | Cytoscape.js（备选 vis-network；统计图用 ECharts） | 大图性能好、交互丰富 |
| 前端框架 | Vue 3 + Vite（纯静态，无构建时服务端） | 生态成熟、包体可控 |
| 存储 | SQLite（抽取结果/校验记录）+ JSON 快照（图谱） | 单文件、易备份 |
| 密钥 | keyring（Windows Credential Manager） | 系统级安全存储 |
| 打包 | PyInstaller（onefile 发布 + onedir 备用） | 单 exe 交付 |
| 测试 | pytest（后端）、Vitest/Playwright（前端）、GitHub Actions CI | 质量保障 |
| 发布 | GitHub Releases + 构建产物哈希 | 开源分发标准做法 |

### 7.3 数据模型（核心表）

```
Project   (id, name, created_at, config_json)
Group     (id, project_id, name, entity_types_json, relation_types_json, extract_config_json)
Document  (id, project_id, group_id, path, title, authors, year, format, status, sha256)
          -- 项目内 path 的文件名唯一，不允许重名；sha256 仅作内容指纹元数据，不驱动去重/合并
Chunk     (id, document_id, page, seq, text, token_count)
Entity    (id, canonical_name, type, description, confidence, aliases_json, source_docs_json)
Relation  (id, source_entity_id, target_entity_id, type, confidence, evidence_json, source_docs_json)
Evidence  (id, entity_id|relation_id, chunk_id, quote, document_id, page)
UserEdit  (id, target_type, target_id, action, payload, created_at)  -- 人工校验记录
```

### 7.4 抽取流水线细节

1. **分块**：按标题层级切分，块 ≤ 3000 tokens，重叠 200 tokens；
2. **Prompt 组装**：系统提示词固定（定义实体/关系类型表、输出 JSON Schema、示例 few-shot、要求返回 canonical name + aliases + evidence 原文摘录）；**新文件场景动态注入「已知实体上下文」**（**同组**既有图谱高频实体与相似候选），引导模型对齐既有命名；
3. **并发调用**：线程池（默认 3），逐块请求；失败重试 2 次 + 指数退避；预算实时核算，超限即停；
4. **解析校验**：JSON Schema 校验；非法输出丢弃该块并记录；
5. **合并去重**：canonical name 归一（大小写/全半角/括号）-> 同名合并 -> 别名命中 -> 相似度（编辑距离/向量）阈值合并，低置信度进「待确认」；
6. **实体对齐（新文件场景）**：抽取后与**同组**图谱对齐匹配，复用既有 canonical name；新类型提示确认后纳入**该组**类型表；
7. **回填入库**：实体/关系写入 SQLite，关联证据；每文档完成后写 checkpoint。

### 7.5 打包与发布方案

- **打包**：PyInstaller onefile；Web 前端构建为纯静态资源随包内嵌；WebView2 运行时检测与按需静默安装；
- **体积优化**：按需裁剪依赖、排除调试符号；目标 exe ≤ 150 MB；
- **CI**：GitHub Actions 在 Windows runner 构建并上传 Release asset + SHA256 清单；
- **签名**：V1.1 起评估代码签名证书，缓解 SmartScreen 提示。

### 7.6 仓库结构（GitHub 开源）

```
DocGraph/
+-- app/                    # 主程序入口与 pywebview 装配
+-- src/docgraph/
|   +-- core/               # 项目模型、存储、配置、密钥管理
|   +-- parsers/            # 文档解析器（pdf/docx/md/txt/html），可插件化
|   +-- extractors/         # LLM 抽取器：provider 抽象 + prompt + JSON Schema
|   +-- graph/              # 图谱构建、去重合并、统计
|   +-- server/             # 内置 HTTP 服务（前端 JSBridge 后端）
+-- web/                    # 前端源码（Vue3 + Vite + Cytoscape.js）
+-- tests/                  # pytest + 前端测试
+-- docs/                   # PRD、架构、用户手册
+-- scripts/                # 打包/发布脚本
+-- examples/               # 示例文档与示例项目
+-- .github/                # ISSUE/PR 模板、CI workflows
+-- README.md               # 中文+英文
+-- LICENSE                 # MIT（宽松许可，公开可商用）
+-- CONTRIBUTING.md
+-- pyproject.toml
```

---

## 8. 数据与隐私

- **默认零上传**：除抽取外，应用不发起任何网络请求；检查更新（FR-902）可关闭；
- **发送内容最小化**：仅发送分块文本与固定提示词，不发送文件路径、项目名等无关信息；设置页可预览「将发送的数据」；
- **本地存储**：所有结果保存在用户项目目录，明文不落盘于临时目录；API Key 走 Credential Manager；
- **删除**：删除文档/项目即级联删除全部派生数据。

---

## 9. 版本规划

| 版本 | 目标 | 主要特性 |
|---|---|---|
| **V1.0 MVP** | 验证核心价值 | 单 exe、**单文件（PDF/DOCX）图构建全链路**、LLM 抽取（OpenAI 兼容）、**文件分组与组级抽取配置**、实体级图谱可视化（按组切换）、**组内新文件增量对齐**、筛选/搜索/下钻、导出 PNG/SVG/JSON/CSV、项目保存与断点续跑、API 配置与 Key 安全存储 |
| **V1.1** | 补齐可信与易用 | 批量导入、MD/TXT/HTML 多格式、人工校验中心（改/合并/忽略）、路径分析、OCR（可选）、BibTeX 元数据导入、代码签名、检查更新、Markdown 摘要报告 |
| **V2.0** | 扩大边界 | 本地模型接入、Web 版/团队共享、插件系统、增量抽取调度、图谱对比（版本 diff） |

---

## 10. 成功指标

**开源/工程指标**
- GitHub：stars ≥ 1k（12 个月）、contributors ≥ 10、issue 首响 ≤ 48h；
- 构建成功率 100%，Release 资产完整性（SHA256）验证通过。

**产品指标（本地工具，默认不做云端埋点，隐私优先）**
- 新手 3 步出图达成率 ≥ 90%（可用性测试样本）；
- 抽取成功率（成功块/总块）≥ 95%；
- 新文件实体对齐率 ≥ 90%（指代既有实体的名称被正确合并到既有节点，而非新建重复节点）；
- 图谱在 5 万节点规模下交互流畅（≥ 30fps）；
- 人工校验后重新抽取的覆盖保持率 ≥ 95%（用户修改不被覆盖）。

---

## 11. 风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| LLM 幻觉产生错误关系 | 图谱可信度下降 | 强制证据字段 + 置信度 + 原文跳转；低置信度进「待确认」；人工校验（V1.1） |
| API 成本不可控 | 用户流失 | 分块缓存、预算硬停止、实时成本展示、并发可配 |
| 抽取结果不一致（同文档多次抽取漂移） | 无法复现 | 固定提示词版本化、temperature 固定 0.1~0.3、JSON Schema 约束、缓存复用 |
| 超长文档超上下文窗口 | 抽取失败 | 分块 + 重叠 + 失败重试；V1.1 增加长文摘要传递上下文 |
| 杀毒软件误报 exe | 安装信任受损 | 代码签名（V1.1）、发布 SHA256、在 README 说明 |
| 扫描版 PDF 无法抽取 | 场景受限 | MVP 明确提示；V1.1 OCR 方案评估 |
| 第三方依赖维护与供应链 | 构建风险 | 依赖锁定、CI 构建、依赖升级策略文档化 |

---

## 12. 待决问题（Open Questions）

| # | 问题 | 建议 |
|---|---|---|
| 1 | 产品名 | ✅ **已定：DocGraph** |
| 2 | 默认推荐 LLM | DeepSeek / GLM（国内低成本和速度）作为默认文档示例；README 列出多家配置 |
| 3 | 是否内置「示例项目」 | 建议内置 1 个示例（含 3-5 篇公开论文 + 预抽取图谱），降低首启门槛 |
| 4 | 图谱规模上限策略 | 建议软上限 5 万实体，超限提示 LOD 渲染说明 |
| 5 | 开源许可证 | ✅ **已定：MIT**（宽松许可、公开可商用）；示例数据单独标注版权 |
| 6 | 首版界面语言 | 建议中英双语界面 + README 双语 |
| 7 | exe 体积上限 | 目标 ≤ 150 MB（用户对「打开即用」的接受边界） |
| 8 | 是否需要 OCR | V1.1 决策，视用户反馈 |
| 9 | 埋点与统计 | 默认零埋点；如需匿名统计需单独征得同意（隐私优先） |
| 10 | 新文件发现的新实体/关系类型如何纳入 | ✅ 按分组确认：确认后纳入**该组**类型表，设置中可改为自动纳入 |

---

## 13. 附录

**术语表**

- **实体（Entity）**：文档中抽取的概念/人物/组织/文献等知识对象；
- **关系（Relation）**：实体间的有向语义联系（提出/基于/改进…）；
- **分块（Chunk）**：文档被切分的抽取单元，携带来源定位；
- **证据（Evidence）**：实体/关系对应的原文摘录与出处；
- **Provider**：LLM 服务商适配层（OpenAI 兼容协议）。

**参考资料**

- OpenAI function calling / JSON Schema 结构化输出文档
- Cytoscape.js / vis-network 图可视化能力对比
- PyInstaller + pywebview 桌面打包最佳实践
- Connected Papers / NotebookLM 产品形态（竞品参照）
